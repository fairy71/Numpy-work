# 1D array
a = np.array([1, 2, 3])

# 2D array
b = np.array([[1, 2], [3, 4]])

# Array of zeros
zeros = np.zeros((2, 3))

# Array of ones
ones = np.ones((3, 3))

# Identity matrix
identity = np.eye(3)

# Range of values
arr = np.arange(0, 10, 2)

# Random values
random = np.random.rand(2, 2)



a = np.array([1, 2, 3])
b = np.array([4, 5, 6])

# Element-wise addition
c = a + b

# Element-wise multiplication
d = a * b

# Dot product
dot = np.dot(a, b)

# Square root
sqrt = np.sqrt(a)

# Mean and sum
mean = np.mean(a)
total = np.sum(a)
